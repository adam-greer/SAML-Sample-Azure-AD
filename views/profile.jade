extends layout
block content
  .profile-container
    h1 Profile
    
    .profile-greeting
      | Hey, #{user.firstName || 'User'}! Welcome back.
    
    .profile-info
      .profile-field
        .profile-field-label Email Address
        .profile-field-value #{user.email || 'Not provided'}
      
      .profile-field
        .profile-field-label First Name
        .profile-field-value #{user.firstName || 'Not provided'}
      
      .profile-field
        .profile-field-label Last Name
        .profile-field-value #{user.lastName || 'Not provided'}
      
      .profile-field
        .profile-field-label Job Title
        .profile-field-value #{user.title || 'Not provided'}
      
      .profile-field
        .profile-field-label Display Name
        .profile-field-value #{user.displayName || 'Not provided'}
    
    .profile-actions
      a(href="/users", class="btn-primary") 👥 View All Users
      if user && user.isAdmin
        a(href="/admin/env", class="btn-admin") ⚙️ SAML Configuration
      a(href="/logout", class="btn-logout") 🚪 Logout
